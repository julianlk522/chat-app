mySQL rPassword: mySqlRoot




Messages:
	-Message_id (INT PK)
	-Sender (FK users.id)
	-Receiver (FK users.id)
	-Content (varchar)
	-Seen (bool)

Users:
	-id (INT PK)
	-name(varchar)
	-username(varchar)
	-password(varchar)

Todos:

Display only convos that contain the user

User login route and context for logged in user

Login and signup pages

Use Tailwind for frontend styling

React Google Login ?

Delete message route / frontend functionality

Assign createdAt field to messages	...	DONE
	-add to controller getMostRecentMessage for all contacts