mySQL rPassword: mySqlRoot




Messages:
	-Message_id (INT PK)
	-Sender (FK users.id)
	-Receiver (FK users.id)
	-Content (varchar)
	-Seen (bool)

Users:
	-id (INT PK)
	-name(varchar)
	-username(varchar)
	-password(varchar)

Todos:

Login and signup pages

Use Tailwind for frontend styling

React Google Login ?

Delete message route / frontend functionality

Assign user data for contact info section (name, pic, personal comment)

Add new contact functionality in MyChats component