mySQL rPassword: mySqlRoot




Messages:
	-Message_id (INT PK)
	-Sender (FK users.id)
	-Receiver (FK users.id)
	-Content (varchar)
	-Seen (bool)

Users:
	-id (INT PK)
	-name(varchar)
	-username(varchar)
	-password(varchar)

Todos:

Display only convos that contain the user ... DONE

User login route and context for logged in user ... DONE

Login and signup pages

Use Tailwind for frontend styling

React Google Login ?

Delete message route / frontend functionality

Assign createdAt field to messages ... DONE
	-add to controller getMostRecentMessage for all contacts ... DONE BETTER

Assign user data for contact info section (name, pic, personal comment)